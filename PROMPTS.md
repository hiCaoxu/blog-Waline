# CaoxuBlog 静态博客 · 分阶段大模型优化提示词

> 用途：把"逐步完善项目"拆成 4 个可独立执行的大模型提示词。每个提示词自带「阶段目标 / 所需上下文说明 / 可执行的具体步骤 / 验收标准」四部分，可直接复制给 WorkBuddy（或任意大模型）逐阶段执行。
> 执行顺序：阶段一 → 二 → 三 → 四。每阶段产出经 `node --check` + 浏览器验证后提交，互不阻塞。
> 项目事实基线（来自代码核查）：零构建纯静态站点；原生 HTML/CSS/JS + hash 路由；内容在 `assets/data.js` 的 `window.SITE_DATA`；外部 CDN 依赖 = marked@12、highlight.js@11.9、hljs 主题 CSS、Waline@3，均无本地副本；评论区为占位（config.js 中 `WALINE_SERVER` 为空）；点赞/阅读量仅 localStorage 演示。

---

## 阶段一 · 必须立即修复（阻塞性问题清零）

**阶段目标**
扫描整个代码库，定位所有会阻止站点正常运行的阻塞性问题——JS 语法/运行时错误、关键功能崩溃、致命逻辑 Bug、明确的安全漏洞；按严重程度排序后逐一修复，并为每项修复给出原因与改动点。本阶段只做"让站点在所有环境下都能正确打开并渲染"的最小必要修复，不新增功能、不改版式观感。

**所需上下文说明**
- 项目类型：零构建纯静态个人博客，直接由浏览器加载 `index.html` 运行，无需 `npm install`、无打包器。
- 技术栈：原生 HTML/CSS/JS（ES5 风格 IIFE），hash 路由（`#/blog` 等），内容集中在 `assets/data.js` 的 `window.SITE_DATA`。
- 关键文件：`index.html`（脚本加载顺序）、`assets/app.js`（`md()` 封装 / `enhanceCodeBlocks` / `mountWaline`）、`assets/search.js`、`assets/style.css`、`assets/config.js`（Waline 占位）。
- 已知最高优先级阻断（供优先核查，以实际扫描为准）：
  1. `assets/app.js` 的 `md()` 依赖外部 `window.marked`（cdnjs v12）；marked 未加载时返回原始 Markdown 字符串，导致四模块正文全部显示 `##`/`**`/`~~~` 裸标记（已在预览复现）。
  2. `highlight.js` 仅走 CDN：离线降级为无高亮纯文本；hljs 主题 CSS 由 `app.js` 动态注入，亦依赖 CDN。
  3. 无 HTML 消毒：`md()` 输出经 `innerHTML` 插入；当前内容由作者可控的 `data.js` 提供风险低，但内容源若扩展到用户输入则存在 XSS，需评估引入 DOMPurify 或明确约束。
- 验收须覆盖三种环境：离线 / CDN 被墙 / 沙箱预览。

**可执行的具体步骤**
1. 用 `node --check` 校验所有 `assets/*.js`，记录并修复任何解析错误。
2. 通读 `index.html` 脚本顺序与 `app.js` 的 `md()`/`enhanceCodeBlocks`/`mountWaline`，列出所有"外部资源缺失即降级为不可用/难看"的脆弱点。
3. 修复 Markdown 渲染阻断：二选一实现——(A) 将 `marked.min.js` 本地化到 `assets/vendor/` 并改 `index.html` 本地引用；(B) 在 `app.js` 内置轻量 Markdown 解析器（覆盖 `##`/`###`/`**`/代码围栏/列表/段落）。确保 `md()` 无外网时仍输出正确 HTML。
4. 对 highlight.js 做同样本地化/降级确认，并验证 hljs 主题 CSS 在明暗主题下都能加载（本地化或内联变量）。
5. 评估 XSS：若本阶段不引入 DOMPurify，在 `md()` 上方注释明确"内容源仅限 data.js 作者输入"，并在 `config.js`/README 标注约束。
6. 逐文件排查运行时崩溃点（`querySelector` 空值、数组未定义、`localStorage` 异常），修复或加保护。
7. 每项修复记录为「问题 → 严重度 → 根因 → 修复 → 影响环境」五元组附在改动说明中。

**验收标准**
- [ ] 所有 `assets/*.js` 通过 `node --check`，无语法错误。
- [ ] 完全断网（禁用外部 CDN）打开 `index.html`，四模块正文仍渲染为标题/加粗/代码块等 HTML，不再出现裸 Markdown 标记。
- [ ] 代码块无 CDN 时至少降级为可读纯文本（带行号与复制按钮），不报错。
- [ ] 控制台无未捕获 red error。
- [ ] 每项修复附严重度与原因说明。
- [ ] 提交信息以 `fix:` 开头，明确仅做阻断修复、未引入新功能。

---

## 阶段二 · 渲染与依赖治理

**阶段目标**
修复一切页面渲染层面的观感/布局问题（布局错乱、样式失效、组件不显示、明暗主题异常），并系统梳理项目"依赖"——本项目的依赖即 `index.html` 引入的外部 CDN 脚本/样式——清理无用或冲突引用、统一版本、确保加载与降级配置正确。在阶段一"能跑"基础上做到"跑得对、跑得稳、依赖可控"。

**所需上下文说明**
- 依赖形态：无 npm 依赖、无构建步骤；所有"依赖"都是 `<script src="https://...">` 形式，集中在 `index.html:65-75` 与 `app.js:24-37`（动态注入 hljs 主题 CSS）。
- 当前 CDN 依赖：marked@12.0.2、highlight.js@11.9.0（含 github/github-dark 主题 CSS）、Waline@3（defer）。marked/hljs 应在阶段一已本地化。
- 渲染相关：`assets/style.css`（CSS 变量主题）、`assets/app.js`（路由渲染）、`assets/search.js`（搜索浮层）、`assets/data.js`。
- 已知渲染关注点（以实际为准）：教程/题库左右栏窄屏是否堆叠；题库答案 CSS `max-height:1200px` 对超长答案可能截断；搜索浮层 z-index 与主题；明暗切换时 hljs 配色同步；导航居中/Logo 同字号间距。

**可执行的具体步骤**
1. 浏览器逐一打开四模块（含详情/目录树/折叠/搜索浮层/明暗切换），记录"布局错乱、样式失效、组件不显示"的清单。
2. 对照 `index.html` 与 `app.js` 梳理外部依赖：删除阶段一已本地化的 CDN 引用（改本地 `assets/vendor/`）；移除确认不用的依赖（暂不启用 Waline 时保留占位并注释启用方式）；统一同类库版本避免冲突。
3. 校验脚本加载顺序：`data.js` → marked(本地) → search.js → highlight.js(本地) → app.js，且 app.js 在 DOM 就绪后执行（脚本置于 body 末尾）。
4. 检查 `style.css` 重复/冲突/未用选择器，清理死样式；确认亮/暗 CSS 变量完整，无某主题下文字不可见。
5. 修复清单问题：超长题库答案（放大 `max-height` 或 JS 测高）、窄屏布局、z-index、明暗同步。
6. 验证降级：禁用某本地/CDN 文件时页面不白屏/不组件消失，仅功能降级。
7. README 补"依赖与本地化说明"，标注每个外部资源本地路径、用途与启停方式。

**验收标准**
- [ ] 四模块在桌面与移动宽度（如 375px）下均无布局错乱、样式失效、组件消失。
- [ ] 所有外部 CDN 依赖已本地化或明确标注可选；`index.html` 无无用/重复/冲突的 `<script>`/`<link>`。
- [ ] 明暗主题切换时正文、代码块、hljs 配色、导航均正确同步。
- [ ] 题库超长答案可完整展开，不被 CSS 高度截断。
- [ ] README 含依赖清单与本地化说明。
- [ ] 提交信息以 `fix:`/`chore:` 开头。

---

## 阶段三 · 功能补全与边界处理

**阶段目标**
对照已实现四模块，梳理缺失的核心交互与业务逻辑，补全"半成品"功能，补齐边界情况与错误提示，使站点从"能看"变"好用、不崩"。新代码须与现有风格（原生 JS、IIFE、hash 路由、`md()` 渲染、localStorage 演示）一致，不引入框架或构建步骤。

**所需上下文说明**
- 已实现：博客（列表/置顶/详情/上下篇/TOC/阅读量/点赞/评论占位/代码高亮）、教程（三层目录树/折叠/面包屑/上下篇/本页TOC）、题库（一级分类/题目折叠/深链）、关于我（md 渲染）、通用（明暗主题/客户端搜索/光标特效）。
- 已知缺口：标签仅展示不可点击筛选；无时间归档页；TOC 平铺正文上方（非 sticky 侧栏）；代码块无折叠长代码；无返回顶部；文章多时 `grid-1` 无分页；阅读量"每次打开+1"含刷新未去重；题库/关于我缺与博客一致的创建/更新时间、阅读量元信息；评论区为占位（Waline serverURL 空）。
- 数据约定：`data.js` 用模板字符串存内容、代码围栏用 `~~~` 避反引号冲突；新增内容沿用 Markdown。

**可执行的具体步骤**
1. 列出"应做但未做"清单（标签筛选、归档、TOC 侧栏、返回顶部、分页/加载更多、阅读量去重、题库/关于我元信息对齐）。
2. 优先补全低成本高价值项：标签点击筛选（复用 `renderBlogList` 按 `tags` 过滤）、归档页（按 `date` 年/月分组）、返回顶部按钮、阅读量会话去重（同 session 不重复 +1）。
3. 半成品收尾：确认置顶/上下篇/TOC 在边界（首篇/末篇/无 TOC/字段缺失）下不报错；缺失字段显示友好占位而非空白崩溃。
4. 错误提示：空数据、加载失败、深链无效（如 `#/blog/不存在`）提供"返回首页/暂无内容"兜底，而非白屏。
5. 如需启用评论：在 `config.js` 填 `WALINE_SERVER` 并验证 Waline 挂载；仍不启用则在 README 标注"评论待接入后端"。
6. 新增函数遵循现有命名与缩进；复用 `md()`/`likeBlock()`/`buildToc()`，不重复造轮子。
7. README 更新"功能清单"与"已知限制"。

**验收标准**
- [ ] 点击博客标签进入该标签筛选列表；归档页按时间正确分组。
- [ ] 长文有返回顶部；阅读量同会话内刷新不重复计数。
- [ ] 题库与"关于我"详情页具备与博客一致的创建/更新时间、阅读量元信息（如适用）。
- [ ] 深链到不存在的文章/分类显示兜底提示而非白屏/报错。
- [ ] 新增代码风格与 `app.js` 一致；无新增框架/构建依赖。
- [ ] README 功能清单与限制已更新。

---

## 阶段四 · 性能、SEO 与部署

**阶段目标**
优化加载速度与运行性能，完善 SEO 与语义化结构，补齐部署所需的构建配置与脚本，使站点达到"可正式托管到腾讯云 COS+CDN / GitHub Pages 并利于收录"的生产级标准。本阶段不动功能逻辑，只做工程化增强。

**所需上下文说明**
- 部署目标：腾讯云 COS 静态网站托管 + CDN（国内需已备案域名）；源码在 GitHub（`hiCaoxu/blog-Waline`）；已有 `deploy/deploy-cos.sh`（依赖 `coscli`，未联调）。
- 性能现状：全部 JS 客户端加载、`data.js` 含全站内容；marked/hljs 阶段一/二应已本地化；无压缩、无缓存头、无懒加载。
- SEO 现状：无 `meta description`、无 Open Graph、无结构化数据、无 sitemap/robots、语义标签（`header/main/footer/nav/article`）可能不全。
- 技术约束：保持零构建纯静态；如需"构建"仅做可选资源压缩/生成 sitemap，不引入框架。

**可执行的具体步骤**
1. 性能：本地化的 `assets/vendor/*.js` 做压缩（生成 `.min` 并引用）；非首屏脚本（search/cursor/Waline）确认 `defer`；大图/代码块按需懒加载；为 COS 配置缓存头（HTML 不缓存、静态资源长缓存+哈希命名）。
2. 语义化与 SEO：补全 `<meta name="description">`、`<meta property="og:*">`、Twitter Card；外壳改为 `<header><nav>`、`<main id="app">`、`<footer>`；文章页注入 JSON-LD 结构化数据（标题/时间/作者）。
3. 站点地图与收录：生成 `sitemap.xml`（列出四模块与所有文章/教程/题目 URL）、`robots.txt`；README 说明随内容更新重新生成。
4. 404 与 SPA 回退：为 COS 配置错误页回退 `index.html`（hash 路由刷新子页不 404）；可选提供 `404.html`。
5. 部署脚本联调：完善 `deploy/deploy-cos.sh`（coscli 同步 `index.html`+`assets/`、刷新 CDN、排除 `.workbuddy/`）；新增可选 GitHub Actions（push 后自动同步 COS + 刷新 CDN）；README 写清"本地预览 → 提交 → 部署"三步。
6. RSS（可选）：生成 `feed.xml` 聚合博客文章。
7. 提交时说明每项优化对加载时间/Lighthouse 的可感知收益。

**验收标准**
- [ ] 本地化 JS 已压缩；首屏外脚本均 `defer`；无渲染阻塞资源，性能分较改造前提升。
- [ ] `index.html` 含 description/og/Twitter Card；外壳用语义标签；文章页有 JSON-LD。
- [ ] 生成 `sitemap.xml` 与 `robots.txt`，URL 覆盖四模块与全部内容页。
- [ ] COS 部署后刷新任意子路由不 404；`deploy-cos.sh` 可实际执行（或 README 标注需先配 coscli 凭证）。
- [ ] README 含完整"本地预览 → 提交 GitHub → 部署腾讯云"流程。
- [ ] 提交信息以 `perf:`/`feat(seo):`/`ci:` 开头。

---

## 使用说明
1. 四个提示词相互独立：每个都自带上下文，可单独复制给大模型执行；建议按一→二→三→四顺序，因后阶段依赖前阶段（如阶段四的"本地化"依赖阶段一/二已完成）。
2. 每阶段结束请人工在浏览器验证验收标准（尤其断网/明暗切换/移动宽度）后再进入下一阶段。
3. 提示词文件本身纳入 Git 版本管理，便于随项目演进修订。
